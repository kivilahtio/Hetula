{
  #Mojolicious secret used to hash credentials. Set this to your deployment
  'secret' => 'testing',
  #In seconds
  'session_expiration' => 60,

  #How many times login can fail before freezing the account
  'max_failed_login_count' => 20,

  #############################################
  ##### Database configuration parameters #####
  ### Can be mysql or mariab or pgsql or sqlite3 or whatever DBI handles
  db_driver => 'SQLite',
  db_name   => ':memory:',
  db_host   => undef,
  db_port   => undef,
  db_user   => 'pastor',
  db_passwd => 'messiah',
  ### Connect using sockets instead? Absolute path to unix-socket
  db_socket => '/var/run/mysql/run.sock', #Just mock this
  #Throw exceptions on error?
  db_raise_error => 1,
  #Print errors to STDERR?
  db_print_error => 1,
  #####                                   #####
  #############################################
}
